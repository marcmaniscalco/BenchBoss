version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.13
    commands:
      - pip install --upgrade pip pipenv
      - pipenv install --deploy

  build:
    commands:
      - pipenv run python tests_integration/qa_smoke_test.py "$CODEBUILD_SRC_DIR_QaStackOutput/output.json"
